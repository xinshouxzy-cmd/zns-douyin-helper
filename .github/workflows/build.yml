name: Build Windows Package

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: write   # 允许创建 Release

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get version
        shell: bash
        run: |
          VER="v2.0.${{ github.run_number }}"
          VER_NUM="2.0.${{ github.run_number }}"
          echo "VERSION=$VER" >> $GITHUB_ENV
          echo "VERSION_NUM=$VER_NUM" >> $GITHUB_ENV
          echo "BUILD_ID=${{ github.run_number }}" >> $GITHUB_ENV
          echo "VERSION = \"$VER\"" > _version.py

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install pyinstaller selenium webdriver-manager PyQt5 pyperclip openpyxl requests

      - name: Download ChromeDriver (Windows)
        shell: pwsh
        run: |
          python -c "
          from webdriver_manager.chrome import ChromeDriverManager
          import shutil, os
          src = ChromeDriverManager().install()
          os.makedirs('runtime', exist_ok=True)
          dst = os.path.join('runtime', 'chromedriver.exe')
          shutil.copy2(src, dst)
          # 也复制到构建根目录（find_chromedriver 会查这里）
          shutil.copy2(src, 'chromedriver.exe')
          print(f'ChromeDriver bundled: {dst}')
          "

      - name: Create data dirs
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path replied_records | Out-Null
          New-Item -ItemType Directory -Force -Path chrome_profiles | Out-Null
          "" | Out-File -FilePath replied_records\.gitkeep -Encoding UTF8
          "" | Out-File -FilePath chrome_profiles\.gitkeep -Encoding UTF8

      - name: Build
        shell: pwsh
        run: |
          pyinstaller --onedir --windowed --name "遵农商_抖音AI工作台" `
            --add-data "config.json;." `
            --add-data "worker.py;." `
            --add-data "updater.py;." `
            --add-data "home_page.py;." `
            --add-data "live_page.py;." `
            --add-data "_version.py;." `
            --add-data "replied_records;replied_records" `
            --add-data "chrome_profiles;chrome_profiles" `
            --add-data "comment_data;comment_data" `
            --add-data "runtime;runtime" `
            --add-binary "chromedriver.exe;." `
            --collect-all PyQt5 `
            --collect-all selenium `
            --collect-all webdriver_manager `
            --hidden-import requests `
            main.py

      - name: Add readme
        shell: pwsh
        run: |
          @"
          遵农商·抖音AI工作台 v$env:VERSION
          ===================================

          【使用前请确保已安装 Chrome 浏览器】
            如未安装，请先下载: https://www.google.cn/chrome/

          【使用方法】
            双击 遵农商_抖音AI工作台.exe 即可运行
            打开后进入工具集合首页（启动页）
            点击「评论私信助手」配置账号并自动回复
            点击「直播助手」配置场景触发规则与知识库后开启监控

          【功能】
            - 多账号抖音私信自动回复
            - 多账号抖音评论自动回复
            - 直播评论关键词 → 自动触发热键切换直播伴侣场景/特效
            - 直播知识库问答（观众提问自动弹出答案）

          遵义农商银行 出品
          "@ | Out-File -FilePath "dist\遵农商_抖音AI工作台\使用说明.txt" -Encoding UTF8

      - name: Package zip
        shell: pwsh
        run: |
          @'
          import shutil, os
          src = r'dist\遵农商_抖音AI工作台'
          dst_name = f'ZNS_Douyin_Helper_v{os.environ["VERSION_NUM"]}'
          shutil.make_archive(dst_name, 'zip', src)
          print('Created: ' + dst_name + '.zip')
          '@ | Out-File -FilePath pack.py -Encoding UTF8
          $env:PYTHONIOENCODING = 'utf-8'
          python pack.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: "遵农商_抖音AI工作台_v${{ env.VERSION_NUM }}"
          path: "dist/遵农商_抖音AI工作台/**"

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "${{ env.VERSION }}-${{ env.BUILD_ID }}"
          name: "遵农商·抖音AI工作台 ${{ env.VERSION }} (Build ${{ env.BUILD_ID }})"
          files: "ZNS_Douyin_Helper_v${{ env.VERSION_NUM }}.zip"
          prerelease: false
        continue-on-error: true
