VERSION = "v2.2.0"
#      (38) v2.2.0 最终交付版：新增启动画面动画（品牌 logo 缩放发光 + 标题渐显 +
#           进度光带 + 淡出进入主界面，约 2.2 秒）；版本号定为最终交付版
#      (37) v2.1.4 界面配色回归深空蓝科技风：启动页三卡片强调色还原为蓝/青/青（#4d94ff/#22d3ee/#34d399），
#           全套品牌图标（主图标+智联/智播/智鉴+手机版）配色由农信绿改为深蓝底+蓝青发光+农商金点缀
#           （图标设计不变，仅换色）；同步手机版图标资产
# 改动:
#       (1) 紧急修复 v2.0.68 打开即闪退：启动页粒子连线绘制时向 QPainter.drawLine 传入 float 坐标，
#           在 Qt 事件循环内抛出 TypeError 导致进程 abort；已改为 int 坐标，多页面渲染测试通过
#       (2) 新增第3个工具「无水印视频下载器」：粘贴抖音分享链接 → 解析无水印原视频 → 一键下载，
#           核心解析逻辑整合自《抖音无水印下载》UniApp 源码（提取视频ID → 解析 _ROUTER_DATA → playwm→play）
#       (3) 三工具风格统一：评论私信助手由浅色微信风改为深色科技风（GitHub Dark 配色 + 蓝色强调），
#           与直播助手、无水印下载器完全统一
#       (4) 启动页再升级：渐变流动品牌标题 + 粒子连线星云 + 双流动光带 + 卡片悬停发光脉冲，
#           新增「无水印下载器」入口卡片，移除占位卡
#       (5) 修复直播助手「返回首页」失效问题（go_home 信号未连接）
#       (6) v2.0.70 修复「评论自动回复抢窗口」：评论回复原先用 ActionChains 移动真实鼠标点击，
#           必然把 Chrome 窗口提到前台抢焦点（多账号多浏览器时尤其明显，快贷中心电脑反馈）。
#           新增「静默模式」（侧边栏开关，默认开启）：评论点击改为 JS 静默点击
#           （elementFromPoint + dispatchEvent，与私信回复同款，不移动鼠标不抢焦点），
#           仅当 JS 点击无效时才回退真实鼠标并日志提示；侧边栏可一键切回兼容模式。
#       (7) v2.0.71 修复「打开软件就弹创建账号」：旧逻辑在 config.json 无账号时启动 300ms 后
#           自动弹出新建账号向导（新装电脑一打开就抢着建号）。改为：打开软件始终停在启动页，
#           只有点击进入「评论私信助手」时才引导创建账号。
#       (8) v2.0.72 修复「直播助手登录后一直卡在等待登录」：原逻辑靠检测评论区元素
#           (.comment-item) 判断是否已登录，直播间无人发评论时永远检测不到 → 3 分钟超时。
#           新增「✅ 我已登录，开始监控」按钮：扫码登录后点击立即跳过等待直接开监控
#           （自动检测评论区仍保留，检测到会自动开始）；轮询间隔 2s 提速到 0.5s 加快响应。
#       (9) v2.0.73 修复「直播助手登录后毫无反应/监测不到评论」：原评论选择器
#           (.comment-item) 系猜测、未实际校验，抖音评论区 DOM 变化后就选不到元素，
#           监控循环等于空转。本次重构评论采集为双通道：
#           ① 选择器链自动探测（.comment-item → .webcast-chatroom___items 等逐级兜底，
#              第一个能命中元素的选择器会被记住并显示在日志里）；
#           ② 注入 MutationObserver 实时捕获页面新增短文本（不依赖任何 class 名，
#             直播间/互动面板 DOM 怎么变都能抓到）。
#           同时新增：20 秒心跳日志（显示累计识别条数，避免"软件没反应"的错觉）、
#           场景触发前先回默认场景热键（v1.5.9 行为）、修复知识库问答 msg_boxes 未初始化崩溃。
#      (10) v2.0.74 根据实测反馈修复：
#           ① 场景触发时序修正：命中关键词 → 发送场景热键（如 ctrl+7）→ 保持 N 秒
#              （默认 7 秒，可配置）→ 自动返回主镜头（小键盘热键，如 num9）；
#              热键发送改用 SendInput + 扫描码（keybd_event 注入事件部分软件不响应，
#              导致直播伴侣全局热键失效），现在任意前台窗口下都能全局触发。
#           ② 关键词分隔兼容全角/半角逗号（，和 , 都视为分隔符）。
#           ③ 相同评论去重改为 30 秒时间窗口：同一条评论不再因去重导致"只识别一次"，
#              不同人/不同时间发送同一关键词可反复触发。
#           ④ 登录态持久化：直播浏览器使用 chrome_profiles/live 固定配置目录，
#              同一台电脑扫码登录一次后不再重复登录收验证码（与评论私信助手一致）。
#           ⑤ 设置面板新增「场景保持(秒)」；提示文字更新热键/分隔符写法。
#      (11) v2.0.75 实测仍热键不触发 + 交互优化：
#           ① 热键发送增加真实按键时序（修饰键按下→按键按下保持 60ms→抬起→修饰键抬起），
#              避免目标软件把瞬时合成事件当噪音丢弃；
#           ② 场景规则改为「一行一个关键词 + 修饰键/按键下拉选择」——不再手输热键防打错，
#              每行带「⚡ 测试」按钮可立即验证直播伴侣是否响应（不用等评论）；
#           ③ 新增「💾 保存设置」按钮：保存后显示"✅ 已保存 HH:MM:SS"并写入 live_config.json，
#              下次打开自动加载；
#           ④ 主镜头回切热键改为小键盘下拉（num0-num9），默认 num9；
#           ⑤ 登录态目录固定为 <软件目录>/chrome_profiles/live（打包版在 exe 旁），
#              启动时日志打印实际路径；换新版本时把该文件夹整个复制过去即可免登录；
#           ⑥ 启动/测试日志提示：若直播伴侣以管理员运行，本软件也需「以管理员身份运行」，
#              否则系统会拦截低权限进程注入的高权限窗口按键。
#      (12) v2.0.76 修复「测试按钮/自动触发完全无反应」的根因：
#           SendInput 的 INPUT 结构体此前漏了联合体（union），结构体大小算出 32 字节，
#           而 Windows x64 要求 40 字节 → SendInput 直接返回 0，按键根本没发出去（无声失败）。
#           本次修正结构体布局（KEYBDINPUT=24 / UNION=32 / INPUT=40，与系统一致），
#           发送时校验 SendInput 返回值，失败自动回退 keybd_event 双通道发送；
#           仍保留真实按键时序（按下保持 60ms）。
#      (13) v2.0.77 修复「监控跑 20 秒后异常退出」+ 日志噪音：
#           ① 心跳日志访问 self._active_sel 时该属性可能尚未初始化（手动确认登录路径
#              会跳过选择器探测）→ AttributeError 导致监控线程崩溃；现于 __init__ 预置 None。
#           ② MutationObserver 会把直播后台界面文案（在线人数/音浪收入/我知道了等）
#              也当评论抓到日志里刷屏；新增 UI_NOISE 黑名单过滤，只显示真正的评论。
#           ③ 采集器注入增加"验证+重试"：返回 None 时复查 window.__liveObserverInstalled，
#              仍失败则降级为仅选择器通道并提示；监控循环内每轮自动补注丢失的观察器。
#      (14) v2.0.78 修复「旧评论能识别、新评论检测不到」：
#           MutationObserver 原先挂在 document.body 上，抖音登录/页面跳转会整页替换 body，
#           观察器随之失效（旧评论是替换前抓到的，之后的新评论全部丢失）。
#           ① 观察目标改为 document.documentElement（页面如何换 body 都不失效），
#              并记录 __liveObserverBody，每轮校验"观察器在 + body 未变"，变了自动重挂；
#           ② 新增全量快照兜底通道：每约 2 秒扫描一次页面叶子级短文本，
#              即使观察器某轮漏抓，新评论下一轮也能被扫到（30 秒去重避免重复）；
#           ③ UI 精简：隐藏「直播后台地址/评论选择器」输入框（自动适配，无需用户配置），
#              「▶ 开始监控」按钮改名「▶ 启动」（扫码登录后点绿色「我已登录」才进入监控）；
#           ④ 噪音过滤补「预览流看播/在线人数音浪收入…」前缀变体。
#      (15) v2.0.79 修复「监控异常：NoneType is not iterable」+ 整体自愈：
#           ① 页面跳转瞬间 execute_script 可能返回 None，_drain_js_comments/_sniff_all
#              直接遍历导致 TypeError 拖死监控线程；现已做 None/非列表防护（一律返回 []）。
#           ② 监控主循环改为"单轮 try/except"：任何一轮采集/处理异常只记录日志并继续，
#              不再出现一次抖动就让整个监控挂掉的情况（日志会提示"已自动继续"）。
#           ③ 噪音过滤补「个人主页」前缀与纯百分比文本（如 100.0% / 0%）。
#      (16) v2.0.80 UI 质感升级（启动门户页重做）+ 配置路径优化：
#           ① 启动页：玻璃拟态卡片（平滑悬停过渡/上浮/辉光/渐变描边）、
#              品牌标题增加柔和光晕与更沉稳的渐变节奏、金色眉题「与遵同行·助农兴企」、
#              环境光晕更柔、光带更细、底部渐隐聚焦内容区——弱化"AI 生成感"，强化品牌质感；
#           ② 规则文件 live_config.json 打包版优先读写 <软件目录(exe旁)>，
#              启动/保存时日志打印实际路径，复制该文件即可整体迁移场景规则。
#      (17) v2.0.81 修复「快贷中心多账号并发时评论回复抢窗口」：
#           根因：7 个浏览器并发时部分 Chrome 窗口被遮挡/最小化，Windows 下 Chrome 会
#           暂停渲染（document.visibilityState 变 hidden），elementFromPoint 拿不到元素
#           → JS 静默点击失败 → 旧代码无条件回退 ActionChains 真实鼠标 → 抢窗口。
#           修复：
#           ① 静默模式下先检查页面可见性，hidden 时直接跳过本次点击（绝不回退抢窗口）；
#           ② JS 点击失败时输出具体原因（坐标无元素/0尺寸/页面不可见），并带 ±3px 微调重试；
#           ③ 真实鼠标回退严格限频：每轮评论循环最多 1 次（_cmt_real_mouse_used）；
#           ④ DOM 搜索到的元素（全部消息/评论筛选/陌生人会话）改用 WebElement.click()
#              （CDP 注入，不移动真实鼠标不抢焦点），仅在失败时才降级坐标点击；
#           ⑤ 私信侧 _enter_stranger 也改为静默点击优先。
#      (18) v2.0.82 修复「直播评论反复重复识别、旧评论几十秒后重新触发」：
#           根因：原去重只按"文本+30秒窗口"，窗口一过被全量扫描的旧评论又会被当成新的。
#           改为「位置增量识别」机制（评论区无时间戳，用列表顺序判断新旧）：
#           ① 主通道按 DOM 顺序采集评论，指纹 = 昵称|内容；
#           ② 新评论 = 出现在上一轮列表「最后一个评论」之后位置的条目——
#              反复扫描到的旧评论不再触发；同一人/不同人新发的评论（出现在新位置）照样触发；
#           ③ 列表被重排/收缩时本轮保守跳过，由观察器+快照兜底通道按指纹窗口补充；
#           ④ 兜底通道去重窗口默认 600 秒（昵称可识别时），昵称未知时收紧到 60 秒；
#           ⑤ 场景保持计时改为"以最后一次触发为准"：新触发使旧返回计时作废
#              （连续多人发关键词时，返回主镜头发生在最后一人触发后 N 秒），日志提示计时重置。
#      (19) v2.0.83 基于 v2.0.79 实测日志再修三处：
#           ① 启动基线：开始监控时先清空观察器缓存、把评论区已有评论标记为"已见"，
#              首轮位置增量只建基线不触发——启动不再把历史评论当新评论逐个触发特效；
#           ② 热键改为监控线程内直接发送（不再经 UI 主线程信号排队），
#              前台/后台/最小化都不受影响；发送失败会打印具体原因
#              （最常见：直播伴侣以管理员运行而本软件没有，SendInput 被系统拦截）；
#           ③ 小键盘/方向键补 KEYEVENTF_EXTENDEDKEY 扩展键标志，
#              避免部分软件区分不出小键盘数字（主镜头回切 num9 更可靠）；
#              测试按钮新增"当前前台窗口"显示，便于排查热键没触发的原因。
#      (20) v2.0.84 修复「日志显示发送成功、但特效只偶尔触发」：
#           根因：注入按键 hold 太短（60ms），直播伴侣若用轮询/前台消息方式监听热键，
#           会漏掉瞬时按键；且注入的按键只会给到"当前前台窗口"。
#           ① 按键按住时长默认加到 0.15 秒（模拟人手按键，设置里可调）；
#           ② 新增「触发前临时激活直播伴侣窗口」开关（默认开）：发送热键前自动找到
#              直播伴侣窗口并带到前台（AttachThreadInput 技巧），发送完立即恢复原前台窗口，
#              既不抢焦点也不影响监控——直播伴侣在前台时注入必达；
#           ③ 每次触发日志附带"发送时前台窗口"，一眼看出按键发给了谁；
#           ④ 找不到直播伴侣窗口时照常发送（兼容真正注册了全局热键的情况）。
#      (21) v2.0.85 修复「识别正常但热键几乎不触发、测试要点好几次才生效」：
#           实测：手动按 Ctrl+6 每次都触发，注入却几乎不触发 → 直播伴侣热键实现
#           （轮询/过滤合成按键）对"瞬时注入"不敏感。
#           ① 按键时序完全模拟人手：按住 Ctrl → 稍等 → 按下数字保持 0.25 秒 →
#              松开数字 → 松开 Ctrl（默认按住时长 0.15 → 0.25 秒）；
#           ② 每次触发连发两遍：第一遍 SendInput（失败自动回退 keybd_event），
#              第二遍 keybd_event——覆盖不同热键实现，场景热键是"切到指定场景"，
#              重复发送无副作用，命中率大幅提升；
#           ③ 测试按钮连发 3 遍，点一次立即见效（不用再狂点）；
#           ④ 每轮发送前先释放可能卡住的 Ctrl/Alt/Shift（防止上一轮失败导致修饰键粘滞）；
#           ⑤ 下拉框屏蔽滚轮：鼠标悬停在"选择数字/修饰键"上滚动不会再误改选项，
#              只能点开选择（避免滑页面时不小心改掉快捷键）。
#      (22) v2.0.86 修复「测试按钮100%触发、但真实开播评论触发不了」+「场景切换打架」：
#           ① 根因：测试按钮用"按住0.25s+连发3遍"，而监控触发还在用旧配置
#              （按住0.15s+连发2遍）——直播伴侣会漏掉短的注入。现已让监控触发
#               与测试按钮参数完全一致（按住≥0.25s+连发3遍），回主镜头热键同样处理；
#           ② 移除"发送前激活直播伴侣窗口"逻辑（测试按钮不激活也能100%触发，
#              证明直播伴侣是真全局热键，激活多余且可能抢焦点干扰用户）；
#           ③ 修复场景切换打架（嘉年华→误回主镜头→抖音1号）：命中新场景时
#              在发送热键之前立即作废旧场景的"返回主镜头"计时——
#              现在任何时刻都以最后一个特效为准，嘉年华进行中被抖音1号打断时，
#              直接切到抖音1号并按抖音1号的时间重新计时返回主镜头，不再误回主镜头。
#      (23) v2.0.87 解决「软件必须在最前端才触发」：
#           直播伴侣需在前台才响应热键（静默注入对它无效），因此采用"触发时置顶+完事恢复"：
#           ① 命中场景时自动找到直播伴侣窗口并置顶到前台 → 发送热键 → 特效保持
#              （默认场景保持时长）→ 自动返回主镜头 → 恢复触发前的原窗口
#              （Word/文件夹等，全程无需手动操作、不打断工作）；
#           ② 未配置"场景保持"时，触发后 5 秒自动恢复原窗口（兜底，不占前台）；
#           ③ 找不到直播伴侣窗口时照常静默发送（兼容真全局热键的机器）；
#           ④ 设置面板新增「触发时自动置顶直播伴侣窗口」开关，可随时关闭。
#      (24) v2.0.88 纠正 v2.0.87 的理解错误：需要在前台的是【本软件】而非直播伴侣。
#           实测：本软件（遵农商工作台）在前台时注入才被系统接受，被 Word/文件夹盖住则失效。
#           修正为：命中场景时把【本软件窗口】置顶到前台 → 发送热键 → 特效保持 →
#           自动返回主镜头 → 恢复触发前的原窗口（Word/文件夹等），全程无需手动操作；
#           未配置场景保持时 5 秒后自动恢复；设置面板开关同步改名。
#      (25) v2.0.89 品牌升级 + 第3工具升级为「智鉴助手」：
#           ① 全局改名：遵农商·智媒工作台（总）+ 智联助手（评论私信回复）+
#              智播助手（直播）+ 智鉴助手（爆款视频分析）+ 智鉴助手·手机版（APK）；
#           ② 智鉴助手（电脑版）：粘贴抖音链接 → 下载无水印视频 → 语音转写文案
#              （百度ASR）→ AI 画面理解（GLM-4.6V）→ 爆款分析报告（DeepSeek），
#              手机版同款能力与密钥；纯音乐/无人声视频自动走画面理解；
#           ③ 智鉴助手页内新增「📱 下载手机版（APK）」按钮：一键导出内置的手机版 APK；
#           ④ 构建时自动内置完整版 ffmpeg（runtime/ffmpeg.exe）与手机版 APK（apk/），
#              电脑无需单独安装 ffmpeg。
#      (26) v2.0.90 统一管理后台：所有软件纳入一套后台管理
#           ① exe 启动时静默上报到统一后台（智媒工作台），进入智联/智播/智鉴分别上报对应软件；
#           ② 后台（采集服务 /admin）新增"按软件统计"：智联助手/智播助手/智鉴助手·电脑版/
#              智鉴助手·手机版各自的安装数、今日/累计用量，设备明细表新增"软件"列；
#           ③ 后台地址可被 config.json 的 backend_url 覆盖（默认内置采集服务隧道地址）。
#      (27) v2.0.91 品牌图标与质感升级（农信绿）：
#           ① 全新品牌图标：深绿渐变（农信绿）+ 金色元素 + 亮绿发光，
#              全套 5 枚（主图标·智媒工作台 / 智联 / 智播 / 智鉴 / 手机版）；
#           ② exe 程序图标（icon_main.ico 16-256 多尺寸）+ 窗口图标；
#           ③ 启动页三卡片用真实图标替换 emoji（icon_zhlian/zhibo/zhijian）；
#           ④ 内置手机版 APK 更新为 v1.0.23（农信绿图标版），智鉴助手页内导出按钮同步。
#      (28) v2.0.92 业务动作统计埋点（向领导证明工具价值）：
#           ① 智联助手：评论回复成功 + 私信回复成功 分别上报（本地累计，每 60 秒批量上报一次）；
#           ② 智播助手：场景特效触发成功 上报；
#           ③ 智鉴助手：视频解析分析完成 上报；
#           ④ 修复 v2.0.90 上报静默失败（updater 用 urllib 却误调 requests，已改回 urllib）；
#           ⑤ 内置手机版 APK 更新 v1.0.24（也带 video_analyze 动作上报）。
#      (29) v2.0.93 智鉴助手体验升级（按反馈）：
#           ① 「文案」改名「🎙 音频转写文案（本视频语音识别）」；
#           ② 分析报告新增「视频基础数据」（点赞/评论/收藏/转发）；
#           ③ 分析链路新增「获取评论区与互动数据」：从云端采集服务拉取真实评论内容，
#              报告新增「评论区观众在聊什么」（引用代表性评论）；
#           ④ 进度条旁新增醒目的步骤提示（⏳ 正在做什么 + 百分比），避免误以为卡死；
#           ⑤ 内置手机版 APK 更新 v1.0.25（安装即上报后台）。
#      (30) v2.0.94 内置手机版 APK 同步 v1.0.26（手机端智鉴助手与电脑端一致的改进：
#           报告含基础数据+评论区分析、文案标题改「音频转写文案」）。
#      (31) v2.0.95 评论真实性修复（绝不编造评论）：
#           ① 评论区未获取到时，报告硬性禁止编造/引用任何评论，第3小节只写
#              「本次未获取到评论区内容」；
#           ② 报告区如实显示「⚠️ 未获取到评论区内容」提示（exe+APK 同步 v1.0.27）。
#      (32) v2.0.96 内置手机版 APK 同步 v1.0.28（等待安抚：三环节动态进度条+实时文案百分比）。
#      (33) v2.0.97 内置手机版 APK 同步 v1.0.29（进度条优化：步骤文案持续显示不闪退；
#           AI拆解环节占约70%进度，解析/下载/转写占30%；每步带步骤号+预计时长说明）。
#      (34) v2.0.98 评论区获取提示优化（同步手机端 v1.0.30）：
#           ① 评论获取失败/为空不再静默，前端明确提示「评论区获取失败（网络或服务未连接）」
#              或「该视频暂未获取到评论区内容」；
#           ② 去掉「报告不会编造评论」表述，简洁如实；
#           ③ 采集超时放宽到 90 秒（手机网络访问隧道可能较慢）。
#      (35) v2.0.99 修复手机端评论永远抓不到的根本原因（同步手机端 v1.0.31）：
#           collectSocial 使用的 HTTP 客户端未设超时，OkHttp 默认读超时仅 10 秒，
#           而服务端抓评论需 20-40 秒 → 手机端每次都被掐断、评论永远为空。
#           已改为长超时（连接20s/读取180s），前端超时同步放宽 150 秒。
#      (36) v2.1.0 行为统计逻辑确认+优化（同步手机端 v1.0.32）：
#           ① 统计全部由「用户主动操作」触发且全程静默（用户无感知）：
#              智鉴=点击「解析并分析」即上报；智联=评论/私信回复成功上报；智播=场景特效触发上报；
#           ② 首次出现的设备记为安装（记录首装时间），后续同设备累积行为+时间；
#           ③ 手机端上报改用长超时客户端+失败重试（解决手机连隧道慢导致漏报）。
#      (37) v2.1.1 内置手机版 APK 同步 v1.0.33（长视频转写防闪退 + 真实进度）：
#           ① 捕获 OutOfMemoryError（原只捕获 Exception，长视频转写 OOM 直接闪退）；
#           ② 转写分块 55s→40s、及时删除临时分块；
#           ③ 转写每完成一段回调真实进度（前端显示「正在转写第 x/y 段」，不再像卡死）。
#      (38) v2.1.2 内置手机版 APK 同步 v1.0.34（长视频提示 + 上报自检）：
#           ① 解析下载后检测视频时长，>2.5 分钟提示「手机内存有限，转写长视频可能闪退，
#              建议改用电脑版」（自然说辞，不再莫名闪退）；
#           ② 新增「上报自检」：页脚连点 5 次版本号，弹出上报结果（设备ID/HTTP/成功失败），
#              用于排查手机上报不到后台的问题。
#      (39) v2.1.3 手机版下载改为二维码（不再内置 APK，包体积减 42MB）：
#           ① 「📱 下载手机版」改为弹出二维码：微信/浏览器扫码即可下载 APK；
#           ② APK 托管在采集服务 /apk/zhijian.apk（家里电脑提供下载，地址可在
#              config.json backend_url 覆盖）；
#           ③ 移除 exe 内置 APK，安装包从约 243MB 降到约 200MB。
